import React from 'react';
import { Text, StyleSheet } from 'react-native';

const DocDisplayScreen = () => {
  return <Text style={styles.text}>DocDisplayScreen</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30
  }
});

export default DocDisplayScreen;